[phases.setup]
nixPkgs = ["python312", "nodejs_20", "uv", "graphifyy"]

[phases.install]
cmds = [
  "cd api && uv sync --frozen",
  "cd web && bun install --frozen-lockfile",
]

[deploy]
startCommand = "cd api && uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
