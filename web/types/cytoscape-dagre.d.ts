/** Minimal type declaration for cytoscape-dagre (loaded via dynamic import). */
declare module "cytoscape-dagre" {
  import type { Ext } from "cytoscape";
  const cytoscapeDagre: Ext;
  export default cytoscapeDagre;
}
